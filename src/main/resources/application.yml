spring:
  application.name: splearn
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  docker:
    compose:
      lifecycle-management: start_only
